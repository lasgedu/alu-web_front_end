function welcome (firstName, lastName) {
    let fullName = firstName + ' ' + lastName;

    function displayFullName() {
        console.log(alert('Welcome ' + fullName + ' !'))
    }

    displayFullName();
}